# DNS Proxy Service 环境变量配置示例
# DNS Proxy Service Environment Variables Example
#
# 将此文件复制为 .env 并根据需要修改
# Copy this file to .env and modify as needed
#
# 注意: 环境变量优先级高于配置文件
# Note: Environment variables take priority over config file

# =============================================================================
# DNS 服务端口 (DNS Service Ports)
# =============================================================================

# UDP DNS 服务端口
DNS_UDP_PORT=53

# DNS over TLS (DoT) 服务端口
DNS_DOT_PORT=853

# DNS over HTTPS (DoH) 服务端口
DNS_DOH_PORT=443

# DNS over QUIC (DoQ) 服务端口
DNS_DOQ_PORT=8853

# =============================================================================
# Web 管理界面 (Web Management Interface)
# =============================================================================

# Web 服务端口
WEB_PORT=8080

# =============================================================================
# TLS 证书 (TLS Certificates)
# =============================================================================

# TLS 证书文件路径
# TLS_CERT_PATH=/path/to/cert.pem

# TLS 私钥文件路径
# TLS_KEY_PATH=/path/to/key.pem

# =============================================================================
# 数据库 (Database)
# =============================================================================

# SQLite 数据库连接 URL
# mode=rwc 表示读写模式，如果文件不存在则创建
DATABASE_URL=sqlite:dns_proxy.db?mode=rwc

# =============================================================================
# 认证 (Authentication)
# =============================================================================

# 管理员用户名
ADMIN_USERNAME=admin

# 管理员密码 (生产环境请务必修改!)
ADMIN_PASSWORD=admin

# =============================================================================
# 日志 (Logging)
# =============================================================================

# 日志文件存储目录
LOG_PATH=logs

# 日志级别: trace, debug, info, warn, error
LOG_LEVEL=info

# 单个日志文件最大大小 (字节)
LOG_MAX_SIZE=10485760

# 日志保留天数
LOG_RETENTION_DAYS=30

# =============================================================================
# 缓存 (Cache)
# =============================================================================

# 缓存默认 TTL (秒)
CACHE_TTL=60

# 缓存最大条目数
CACHE_MAX_ENTRIES=10000

# =============================================================================
# 上游 DNS 服务器 (Upstream DNS Servers)
# =============================================================================

# 上游 DNS 服务器列表 (逗号分隔)
UPSTREAM_SERVERS=8.8.8.8:53,8.8.4.4:53
